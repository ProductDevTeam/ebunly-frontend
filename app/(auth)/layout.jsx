import "../globals.css";

export const metadata = {
  title: "Sign Up",
  description: "Create a free Ebunly account",
};

export default function AuthLayout({ children }) {
  return <>{children}</>;
}
